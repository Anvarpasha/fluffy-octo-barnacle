<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anvarpasha.avtogarage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".data.network.persistence.GarageApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.profile.ProfileActivity" android:label="@string/back"/>
        <activity android:name=".ui.dashboard.MainActivity" android:label="" />
        <activity android:name=".ui.orderDetail.OrderDetail" android:label="@string/back" />
        <activity android:name=".ui.search.SearchActivity" android:label="@string/back"/>
        <activity android:name=".ui.searchList.SearchResultActivity" android:label="@string/back"/>
        <activity
            android:name=".ui.auth.AuthActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>